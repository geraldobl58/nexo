# Values for Staging environment
replicaCount: 1

image:
  repository: ghcr.io/geraldobl58/nexo-be
  tag: "staging-ac9be10f3c056ac3d986cd68f32c4b014e3858ae"
  pullPolicy: Always

imagePullSecrets:
  - name: ghcr-secret

ingress:
  enabled: true
  className: "nginx"
  annotations:
  hosts:
    - host: staging-be.nexo.local
      paths:
        - path: /
          pathType: Prefix

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 10m
    memory: 64Mi

autoscaling:
  enabled: false

env:
  NODE_ENV: production
  PORT: "3000"
  LOG_LEVEL: info
  SWAGGER_ENABLED: "true"
  METRICS_ENABLED: "true"
  KEYCLOAK_URL: "http://staging-auth.nexo.local"
  KEYCLOAK_INTERNAL_URL: "http://nexo-auth-staging:8080"
  KEYCLOAK_REALM: nexo

podDisruptionBudget:
  enabled: false

podAnnotations:
  app.kubernetes.io/commit: "ac9be10"

metrics:
  enabled: false
  path: /metrics
  serviceMonitor:
    enabled: false
