# ============================================================================
# ArgoCD Application - Nexo Frontend (Local)
# ============================================================================
# Image Updater monitora DockerHub e atualiza automaticamente
# ============================================================================

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nexo-fe-local
  namespace: argocd
  labels:
    app.kubernetes.io/name: nexo-fe
    environment: local
  annotations:
    # ArgoCD Image Updater - monitora DockerHub automaticamente
    # Monitora a tag 'develop' e atualiza quando o digest muda
    argocd-image-updater.argoproj.io/image-list: nexo-fe=docker.io/geraldobl58/nexo-fe:develop
    argocd-image-updater.argoproj.io/nexo-fe.update-strategy: digest
    argocd-image-updater.argoproj.io/write-back-method: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: nexo-local

  source:
    repoURL: https://github.com/geraldobl58/nexo.git
    targetRevision: develop
    path: infra/helm/nexo-fe
    helm:
      valueFiles:
        - values.yaml
        - values-local.yaml
      parameters:
        - name: image.repository
          value: "docker.io/geraldobl58/nexo-fe"
        - name: image.tag
          value: "develop"
        - name: image.pullPolicy
          value: "Always"
        - name: ingress.hosts[0].host
          value: "nexo.local"

  destination:
    server: https://kubernetes.default.svc
    namespace: nexo-local

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
