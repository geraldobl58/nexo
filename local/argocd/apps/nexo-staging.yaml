# ============================================================================
# Nexo Platform - ArgoCD Applications - Ambiente Staging
# ============================================================================
# Branch: staging
# Domínios: staging.nexo.local, staging.api.nexo.local, staging.auth.nexo.local
# ============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nexo-be-staging
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: nexo-be=docker.io/geraldobl58/nexo-be:staging
    argocd-image-updater.argoproj.io/nexo-be.update-strategy: digest
    argocd-image-updater.argoproj.io/nexo-be.pull-secret: secret:argocd/dockerhub-creds#creds
    argocd-image-updater.argoproj.io/write-back-method: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: nexo-be
    environment: staging
spec:
  project: nexo-staging
  source:
    repoURL: https://github.com/geraldobl58/nexo.git
    targetRevision: staging
    path: infra/helm/nexo-be
    helm:
      valueFiles:
        - values.yaml
        - values-local.yaml
      parameters:
        - name: image.repository
          value: docker.io/geraldobl58/nexo-be
        - name: image.tag
          value: staging
        - name: image.pullPolicy
          value: Always
        - name: ingress.hosts[0].host
          value: staging.api.nexo.local
        - name: imagePullSecrets[0].name
          value: dockerhub-creds
  destination:
    server: https://kubernetes.default.svc
    namespace: nexo-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nexo-fe-staging
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: nexo-fe=docker.io/geraldobl58/nexo-fe:staging
    argocd-image-updater.argoproj.io/nexo-fe.update-strategy: digest
    argocd-image-updater.argoproj.io/nexo-fe.pull-secret: secret:argocd/dockerhub-creds#creds
    argocd-image-updater.argoproj.io/write-back-method: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: nexo-fe
    environment: staging
spec:
  project: nexo-staging
  source:
    repoURL: https://github.com/geraldobl58/nexo.git
    targetRevision: staging
    path: infra/helm/nexo-fe
    helm:
      valueFiles:
        - values.yaml
        - values-local.yaml
      parameters:
        - name: image.repository
          value: docker.io/geraldobl58/nexo-fe
        - name: image.tag
          value: staging
        - name: image.pullPolicy
          value: Always
        - name: ingress.hosts[0].host
          value: staging.nexo.local
        - name: imagePullSecrets[0].name
          value: dockerhub-creds
  destination:
    server: https://kubernetes.default.svc
    namespace: nexo-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nexo-auth-staging
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: nexo-auth=docker.io/geraldobl58/nexo-auth:staging
    argocd-image-updater.argoproj.io/nexo-auth.update-strategy: digest
    argocd-image-updater.argoproj.io/nexo-auth.pull-secret: secret:argocd/dockerhub-creds#creds
    argocd-image-updater.argoproj.io/write-back-method: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: nexo-auth
    environment: staging
spec:
  project: nexo-staging
  source:
    repoURL: https://github.com/geraldobl58/nexo.git
    targetRevision: staging
    path: infra/helm/nexo-auth
    helm:
      valueFiles:
        - values.yaml
        - values-local.yaml
      parameters:
        - name: image.repository
          value: docker.io/geraldobl58/nexo-auth
        - name: image.tag
          value: staging
        - name: image.pullPolicy
          value: Always
        - name: ingress.hosts[0].host
          value: staging.auth.nexo.local
        - name: imagePullSecrets[0].name
          value: dockerhub-creds
        - name: managementPort
          value: "9000"
        - name: livenessProbe.httpGet.port
          value: "9000"
        - name: readinessProbe.httpGet.port
          value: "9000"
  destination:
    server: https://kubernetes.default.svc
    namespace: nexo-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
